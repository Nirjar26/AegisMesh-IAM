apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Bastion-app-monitor
  namespace: observability
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: backend
  namespaceSelector:
    matchNames:
    - Bastion
  endpoints:
  - port: http
    path: /metrics
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: security-engine-monitor
  namespace: observability
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: security-engine
  namespaceSelector:
    matchNames:
    - Bastion
  endpoints:
  - port: metrics
    path: /metrics/
