apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../manifests
  - ../../networkpolicies
  - sealedsecret-Bastion.yaml

patches:
  - path: patch-backend-image.yaml
  - path: patch-frontend-image.yaml
  - path: patch-resources.yaml

images:
  - name: Bastion-security-engine
    newName: Bastion-security-engine
    newTag: latest
