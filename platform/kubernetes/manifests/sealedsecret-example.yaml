apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: Bastion-secrets
  namespace: Bastion
spec:
  encryptedData: {}

# NOTE:
# This file is a placeholder example. You must generate a real SealedSecret using the
# `kubeseal` client and the installed SealedSecrets controller public key.
# Example:
#  kubeseal --controller-namespace kube-system --format yaml < platform/kubernetes/manifests/secret.yaml > platform/kubernetes/overlays/prod/sealedsecret-Bastion.yaml
